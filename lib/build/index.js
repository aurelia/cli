"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.src = src;
exports.createLoaderCode = createLoaderCode;
exports.createLoaderConfig = createLoaderConfig;
exports.bundle = bundle;
exports.dest = dest;
exports.clearCache = clearCache;
const node_stream_1 = require("node:stream");
const bundler_1 = require("./bundler");
const package_analyzer_1 = require("./package-analyzer");
const package_installer_1 = require("./package-installer");
const utils_1 = require("./utils");
const fs = __importStar(require("node:fs"));
let bundler;
let project;
let isUpdating = false;
async function src(p) {
    if (bundler) {
        isUpdating = true;
        return Promise.resolve(bundler);
    }
    project = p;
    const b = await bundler_1.Bundler.create(project, new package_analyzer_1.PackageAnalyzer(project), new package_installer_1.PackageInstaller(project));
    return bundler = b;
}
;
async function createLoaderCode(p) {
    const createLoaderCode = (await Promise.resolve().then(() => __importStar(require('./loader')))).createLoaderCode;
    project = p || project;
    await buildLoaderConfig(project);
    const platform = project.build.targets[0];
    return createLoaderCode(platform, bundler);
}
;
async function createLoaderConfig(p) {
    const createLoaderConfig = (await Promise.resolve().then(() => __importStar(require('./loader')))).createLoaderConfig;
    project = p || project;
    await buildLoaderConfig(project);
    const platform = project.build.targets[0];
    return createLoaderConfig(platform, bundler);
}
;
function bundle() {
    return new node_stream_1.Transform({
        objectMode: true,
        transform: function (file, encoding, callback) {
            callback(null, capture(file));
        }
    });
}
;
function dest(opts) {
    return bundler.build(opts)
        .then(() => bundler.write());
}
;
function clearCache() {
    // delete cache folder outside of cwd
    return fs.promises.rm(utils_1.cacheDir, { recursive: true, force: true });
}
;
async function buildLoaderConfig(p) {
    project = p || project;
    let configPromise = Promise.resolve();
    if (!bundler) {
        //If a bundler doesn't exist then chances are we have not run through getting all the files, and therefore the "bundles" will not be complete
        configPromise = configPromise.then(() => {
            return bundler_1.Bundler.create(project, new package_analyzer_1.PackageAnalyzer(project), new package_installer_1.PackageInstaller(project)).then(b => { bundler = b; });
        });
    }
    await configPromise;
    return bundler.build();
}
function capture(file) {
    // ignore type declaration file generated by TypeScript compiler
    if (file.path.endsWith('d.ts'))
        return;
    if (isUpdating) {
        bundler.updateFile(file);
    }
    else {
        bundler.addFile(file);
    }
}
//# sourceMappingURL=index.js.map