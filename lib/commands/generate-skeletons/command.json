{
  "name": "generate-projects",
  "description": "Generates skeleton applications in the current directory.",
  "flags": [
    {
      "name": "definitions",
      "description": "optionally provide a file which contains project definitions",
      "type": "string"
    }
  ]
}
