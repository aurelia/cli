{
  "dependencies": [{
    "name": "bootstrap",
    "path": "../node_modules/bootstrap/dist",
    "main": "js/bootstrap.min.js",
    "deps": ["jquery"],
    "exports": "$",
    "resources": [
      "css/bootstrap.css"
    ]
  }],
  "tutorial": [
    "1. in main.js add \"import 'bootstrap';\"",
    "2. in app.html add \"<require from='bootstrap/css/bootstrap.css'></require>\"",
    "3. Make sure that jQuery is installed ('au install jquery') and the dependency definition in aurelia.json is preceding the bootstrap dependency since order matters!"
  ]
}